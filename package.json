{
  "name": "tezsend",
  "version": "1.0.0",
  "description": "TezSend - Fintech App",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd web && npm install && npm run build",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
